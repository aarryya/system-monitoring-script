# System Monitoring Script 🖥️

A simple **system monitoring script** for Windows using **Git Bash** and **PowerShell**.  
It provides real-time information about CPU, memory, disk usage, and top processes — similar to Linux `top` command.

---

 **Features** ✨

- **CPU Usage**: Shows average CPU load percentage.
- **Memory Usage**: Displays total, free, and used memory with percentage.
- **Disk Usage**: Shows usage of C: drive with used GB and percentage.
- **Top 5 Processes by Memory**: Lists the five heaviest processes currently running.
- **Windows Git Bash Compatible**: Works smoothly with Git Bash without errors.
- **Lightweight & No Installation Required**: Just run the script!

---

 **Tech Stack** 🛠️

- **Shell Script** (`.sh`)
- **PowerShell Commands**
- **Git Bash** (Windows MINGW64)
- Optional: Can be run directly in **PowerShell** as well

---

## **Usage** 🚀

1. Clone the repository:

```bash
git clone https://github.com/aarryya/system-monitoring-script.git
cd system-monitoring-script
